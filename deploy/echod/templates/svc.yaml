apiVersion: v1
kind: Service
metadata:
  name: {{.Values.app}}
  labels:
    app: {{.Values.app}}
    project: {{.Values.project}}
spec:
  ports:
  - name: grpc
    port: 8443
    targetPort: grpc
  - name: json
    port: 9443
    targetPort: json
  selector:
    app: echod